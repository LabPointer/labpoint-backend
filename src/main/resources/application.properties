spring.application.name=labpoint
server.port=3001

# --- Security ---
api.security.token.secret=${JWT_SECRET:my-secret-key}
api.security.token.age=120

# --- Datasource ---
spring.datasource.url=jdbc:postgresql://localhost:5432/labpoint
spring.datasource.username=labpoint
spring.datasource.password=labpoint
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# --- Flyway ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=true
spring.flyway.out-of-order=true

# --- OpenAPI / Swagger / Scalar ---
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.scalar.path=/docs
scalar.path=/docs